{
  "name": "marcio-binsely-digital",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de gestão de campanha política — Márcio Binsely Digital",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "setup": "npm run install:all && npm run db:up && npm run prisma:migrate && npm run seed",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n backend,frontend -c red,blue \"npm:dev:backend\" \"npm:dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
